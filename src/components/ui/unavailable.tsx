import { Card, CardBody } from "@nextui-org/react";
export default function Unavailable() {
  return <Card className="mx-6 md:mx-10">
  <CardBody>
    <p >Unavailable at the moment!</p>
  </CardBody>
</Card>
}